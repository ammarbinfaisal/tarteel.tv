# Public base URL used to resolve clip variant r2Key -> playable URL.
# Example: https://cdn.yourdomain.com
R2_PUBLIC_BASE_URL=

# Required for `bun run clip -- ingest` uploads (Cloudflare R2 S3-compatible API)
R2_BUCKET=
# Example: https://<accountid>.r2.cloudflarestorage.com
R2_ENDPOINT=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=

# Optional upload tuning:
# R2_UPLOAD_ATTEMPTS=3
# R2_MAX_ATTEMPTS=5
# R2_PART_SIZE_MB=10
# R2_QUEUE_SIZE=4
